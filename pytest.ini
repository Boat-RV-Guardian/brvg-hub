; Python tests here are ONLY the Home Assistant integration's — the collectors are TypeScript and
; shell, tested by ci.yml. testpaths keeps a bare `pytest` from wandering into node_modules.
[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q
