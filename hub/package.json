{
  "name": "brvg-hub",
  "version": "0.6.0",
  "license": "Apache-2.0",
  "private": true,
  "description": "Boat & RV Guardian on-site hub — aggregates local sensor reports into one cloud roll-up (Pi / Docker / desktop).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
